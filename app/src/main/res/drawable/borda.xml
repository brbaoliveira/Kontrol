<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/>   <!-- Cor do botão -->
    <corners android:radius="10dp"/>  <!-- Arredondamento -->
    <stroke android:width="2dp"  />         <!-- Espessura da borda -->
    <stroke android:color="@color/cinza_claro"/> <!-- Cor da borda -->
</shape>